<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ng.dreammesh.app.dreamtunes">

    <!-- android:windowSoftInputMode="adjustNothing" -->
    <!-- change the app theme's name(Appname) to noaction bar( AppTheme.NoActionBar ) -->

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        >

        <activity
            android:name=".custom.activities.welcome"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".custom.activities.loginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".custom.activities.registerActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".custom.activities.dtmainPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".custom.activities.songplay" />
        <activity android:name=".custom.activities.charts"

            ></activity>
    </application>

</manifest>